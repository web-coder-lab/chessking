# Part 24 — Clear error messages

`friendlyError()` maps codes to plain English:
- network / server wake-up
- email_send_failed
- invalid credentials, username/email taken, weak password
- rate limit, session expired
- already claimed, insufficient coins
