# Workflows

- `build-android-apk.yml` — **legacy Capacitor WebView** (deprecated for product)
- Product APK will use **android-native/** Compose project (Phase 2–5)
- `build-api-image.yml` — API Docker image
