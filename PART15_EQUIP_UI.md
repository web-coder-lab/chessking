# Part 15 — Equip real-time UI

- `utils/avatar.js` maps `avatar_id` / `banner_id` → emoji
- TopBar shows equipped avatar emoji (not broken PNG)
- Profile banner + avatar from user fields
- Inventory equip → `refreshUser()` so TopBar updates immediately
